// Next.js 14's App Router ships a React canary that provides `useFormState`
// and `useFormStatus`. Their type declarations live in the canary entry point,
// so pull them in project-wide.
/// <reference types="react-dom/canary" />

export {};
